import 'package:flutter/material.dart';

class AboutScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This App is exclusively design for latest News Headlines', style: TextStyle(fontSize: 20)),
    );
  }
}
